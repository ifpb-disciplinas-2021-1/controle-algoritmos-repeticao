// O que esse algoritmo faz?
#include <stdio.h>
int main(){
  int numero, contador=1;
  while (numero==contador){ //enquanto o numero for igual a contador
     printf("Digite um número: ");
     scanf("%i", &numero);
  }
  return 0; 
}